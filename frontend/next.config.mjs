/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["127.0.0.1", "localhost"], // Permite cargar imágenes desde 127.0.0.1 y localhost
  },
};

export default nextConfig;
